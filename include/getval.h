#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int getVal(int nextplayer);
int huoSi(int x, int y, int player);
int chongSi(int x, int y, int player);
int huoSan(int x, int y, int player);
int mianSan(int x, int y, int player);
int huoEr(int x, int y, int player);
int mianEr(int x, int y, int player);
